# DECISION LOG — MarkItDown

## 2026-07-27 — Workspace Alignment to DCOM Continuity Standards
*   [agy/a7-markitdown/gemini]: Aligned workspace to DCOM continuity standards (Mirrors, CONTINUE.md, DECISION-LOG.md, Doctrine Manifest).

## 2026-07-30 — Shared OPICS Document-Distillation Boundary
*   [codex/a7-markitdown]: Reused OutPace's `feketerj/MarkItDown` repository and the TDP lane's isolated, proven DeepDoc OCR shim instead of creating pipeline-local PDF parsers. This repository is the OPICS integration/control surface; upstream packages are implementation dependencies, not pipeline entrypoints. The shared boundary emits immutable Markdown, positioned OCR, and provenance receipts; domain extraction and promotion remain with the consuming pipeline. A non-symlink input beneath an explicitly declared pipeline-owned root moves to recoverable quarantine after its receipt lands. Deletion is separately fail-closed: it requires a consuming-pipeline authorization bound to terminal disposition and downstream evidence hashes, followed by revalidation in the governed reaper.
*   [codex/a7-markitdown]: Capability band is deliberately `deepdoc-ocr`. Full RAGFlow parsing and table-structure recognition remain unverified and are not promoted.
*   [codex/a7-markitdown]: Unattended generic conversion is isolated from the orchestrator behind a bounded two-lane DBR drum. Nested/high-expansion archives and partial writes fail closed; wall time and process-group resources are capped. Generic non-PDF output is truth-banded `parser_output_only`, not full-document. Parallel calls on one pipeline-owned source serialize through a private source lock.
*   [codex/a7-markitdown]: Cleanup authorization is evidence-bound, not assertion-bound. The writer hashes a durable downstream artifact and the reaper reopens and rehashes it before deletion; missing, mutable, symlinked, raw-root-local, or changed evidence refuses cleanup.
*   [codex/a7-markitdown]: Parser provenance never attributes dirty code to the last commit. Receipts carry the current HEAD, clean/dirty/unresolved state, and exact hashes for every A7 distillation code artifact; `owner_commit` is populated only when those files are clean and reproducible from that commit.
